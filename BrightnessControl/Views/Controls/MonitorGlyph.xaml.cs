namespace BrightnessControl.Views.Controls;

/// <summary>Small vector monitor icon (screen + stand), drawn from shapes so it needs no image asset.</summary>
public partial class MonitorGlyph : System.Windows.Controls.UserControl
{
    public MonitorGlyph()
    {
        InitializeComponent();
    }
}
